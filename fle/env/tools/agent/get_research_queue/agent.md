## get_research_queue

`get_research_queue()`

Returns queue entries (one-based index, name, level), current technology, current progress and tick. The native queue includes the current research. Reading never starts or changes research.
